#pragma once

#define P2P_DILUTION_RATE (5000000) // extra block reward = (max_supply - current_supply) / this
#define DICE_MAX_WAIT_BLOCKS (50) // extra block reward = (max_supply - current_supply) / this
